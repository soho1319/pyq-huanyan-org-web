---
title:部署指南 · 从0 到 pyq.huanyan.org 双站部署
tags: [web/部署/指南]
created:2026-06-09
---

# 🚀部署指南 · 从0 到 pyq.huanyan.org

>跟着做，预计 **2-3 小时** 完成整个部署。

---

## 📋前置准备（10 分钟）

###1. 你需要准备的账号 /工具

|准备项 |用途 | 没准备的 →怎么办 |
|---|---|---|
| ✅ Node.js18+ |跑 Quartz | [下载](https://nodejs.org/) |
| ✅ Git | clone Quartz | [下载](https://git-scm.com/) |
| ✅ GitHub账号 |托管代码 + CF Pages联动 | [注册](https://github.com/) |
| ✅ Cloudflare账号 | 国际站 | [注册](https://dash.cloudflare.com/sign-up) |
| ✅腾讯云账号 + EdgeOne | 国内站 | [注册](https://console.cloud.tencent.com/) |
| ✅域名 `huanyan.org` 在 CF 或 DNSPod | 加子域 `pyq.huanyan.org` | 已拥有 |
| ⏳ AI API key（先不做也行） |调 AI | 见 [[../内容营销朋友圈助手/30 - AI Prompt库（10场景）]] |

###2.域名解析准备

把 `pyq.huanyan.org` 加好：
- **如果 huanyan.org 在 Cloudflare**：CF Dashboard → DNS → 加 CNAME `pyq` → `pyq-huanyan.pages.dev`（CF Pages 默认域名）
- **如果 huanyan.org 在 DNSPod**：DNSPod → 加 A记录 `pyq` → EdgeOne分配的 IP

> 💡 **更优雅**：两个平台都用，先部署完，DNS 后切。

---

## 🛠️Step1：本地搭建（30 分钟）

###1.1克隆这个项目到本地

```bash
# 在你想放项目的目录（不要放在 C:\Users路径下，避免权限问题）
cd D:\Projects
git clone <你的GitHub repo URL> pyq-huanyan-org-web
cd pyq-huanyan-org-web
```

>第一次先在 GitHub 建一个 repo（命名 `pyq-huanyan-org-web`），本地初始化后 push上去。
> 然后克隆 = 有完整历史。

###1.2跑 setup脚本（自动安装）

**Windows**：
```bat
scripts\1-setup.bat
```

**Mac/Linux**：
```bash
chmod +x scripts/*.sh
./scripts/1-setup.sh
```

这个脚本会自动：
1.克隆 Quartz v4 到 `vendor/quartz/`
2. 安装 npm依赖
3. 把 `quartz.config.ts`复制到 Quartz
4. 把 `components/`复制到 Quartz
5. 把 `.env.example`复制成 `.env`

###1.3复制 vault 内容到 Quartz

```bash
# Windows
xcopy /E /I "D:\Backup\Documents\我的Obsidian文档\一人公司\内容营销朋友圈助手" "vendor\quartz\content"
xcopy /E /I "D:\Backup\Documents\我的Obsidian文档\内容营销朋友圈\doc" "vendor\quartz\content\课程原文"

# Mac/Linux
cp -r "../我的Obsidian文档/一人公司/内容营销朋友圈助手" vendor/quartz/content/
cp -r "../我的Obsidian文档/内容营销朋友圈/doc" vendor/quartz/content/课程原文/
```

###1.4 本地启动看效果

```bat
scripts\2-dev.bat
```

浏览器打开 `http://localhost:8080`，应该能看到所有笔记渲染成网站。

---

## 🤖Step2：AI API集成（30 分钟）

###2.1选 AI 模型 +拿 API key

|平台 | 推荐模型 | API文档 | key 在哪拿 |
|---|---|---|---|
| **国际站** | Claude Sonnet / Haiku | [docs.anthropic.com](https://docs.anthropic.com/) | console.anthropic.com → Settings → API Keys |
| **国际站备选** | GPT-4o-mini（便宜） | [platform.openai.com](https://platform.openai.com/docs) | platform.openai.com → API keys |
| **国内站主推** | minimax M3 |找 M3官方 API文档 | 待你确认 |
| **国内站备选** | DeepSeek | [api-docs.deepseek.com](https://api-docs.deepseek.com/) | platform.deepseek.com → API keys |
| **国内站备选** | Kimi（月之暗面） | [platform.moonshot.cn](https://platform.moonshot.cn/) | platform.moonshot.cn → API Keys |

###2.2配 `.env`

```bash
#复制模板
cp .env.example .env

# 编辑 .env
notepad .env # Windows
nano .env # Mac/Linux
```

填入：
```bash
# === 国际站 (CF Pages) ===
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxx #备选

# === 国内站 (EdgeOne Pages) ===
MINIMAX_API_KEY=your-minimax-m3-api-key
MINIMAX_BASE_URL=https://api.minimax.chat/v1 # 或 DeepSeek / Kimi 的 URL
MINIMAX_MODEL=M3 # 或 deepseek-chat / moonshot-v1-8k

# ===通用 ===
SITE_URL=https://pyq.huanyan.org
FREE_TIER_DAILY_LIMIT=50 # 免费用户每天50 次
```

###2.3 本地测试 AI 生成

回到 `http://localhost:8080`，打开 [[../内容营销朋友圈助手/30 - AI Prompt库（10场景）]]，点 **"🤖 AI 生成"**按钮 →填表单 → 看返回结果。

如果失败：
- 检查 `.env` 里 API key格式
- 看控制台日志（`scripts/2-dev.bat` 输出）
- 看浏览器 DevTools → Network → `/api/generate` 请求的报错

---

## ☁️Step3：部署到 Cloudflare Pages（国际站）（30 分钟）

###3.1推到 GitHub

```bash
git add .
git commit -m "feat: initial pyq.huanyan.org setup"
git push origin main
```

> ⚠️ `.env` 不要 push！已在 `.gitignore` 里。

###3.2 CF Pages 接 GitHub

1.登录 [dash.cloudflare.com](https://dash.cloudflare.com/) → Workers & Pages → Create application → Pages → Connect to Git
2.选你的 repo `pyq-huanyan-org-web`
3. 配置：
 - **Framework preset**：None
 - **Build command**：`cd vendor/quartz && npm run build`
 - **Build output directory**：`vendor/quartz/public`
 - **Root directory**：`/`（留空）
4. **Environment variables**（重要！）：
 - `ANTHROPIC_API_KEY` = `sk-ant-xxx`
 - `OPENAI_API_KEY` = `sk-xxx`（如果用）
 - `FREE_TIER_DAILY_LIMIT` = `50`
5. 点 **Save and Deploy**

###3.3 配置自定义域名

1. CF Pages →你的项目 → Custom domains → Set up a custom domain
2. 输入 `pyq.huanyan.org`
3. CF 自动加 DNS记录 +配 HTTPS（几秒到几分钟）

###3.4验证国际站

打开 https://pyq.huanyan.org → 应该能看到网站

> 国内访问慢没关系，国内站会更快。

###3.5 自动部署（GitHub Actions）

我已经写好 `.github/workflows/deploy.yml`，push 到 `main` 分支会自动触发 CF Pages重新部署。

---

## 🇨🇳Step4：部署到 EdgeOne Pages（国内站）（30 分钟）

###4.1 安装 EdgeOne CLI

```bash
npm install -g edgeone
```

###4.2登录腾讯云

```bash
edgeone login
#浏览器扫码登录
```

###4.3 配置 `edgeone.json`

在 `vendor/quartz/`目录下创建 `edgeone.json`：
```json
{
 "name": "pyq-huanyan-org",
 "framework": "quartz",
 "buildCommand": "npm run build",
 "outputDirectory": "public",
 "env": {
 "MINIMAX_API_KEY": "@secret:minimax_api_key",
 "MINIMAX_BASE_URL": "https://api.minimax.chat/v1",
 "MINIMAX_MODEL": "M3",
 "FREE_TIER_DAILY_LIMIT": "50"
 }
}
```

###4.4 在 EdgeOne 控制台存 secret

1.腾讯云控制台 → EdgeOne → Secrets management
2. 加 secret：`minimax_api_key` =你的 M3 key

###4.5部署

```bat
scripts\4-deploy-edgeone.bat
```

或者手动：
```bash
cd vendor/quartz
npm run build
edgeone deploy
```

###4.6 配置域名

EdgeOne 控制台 →域名管理 → 添加 `pyq.cn.huanyan.org`（或别的子域，避开和 CF冲突）

> 💡 国内国外域名可以分开：
> - `pyq.huanyan.org` → CF Pages（国际站）
> - `pyq-cn.huanyan.org` → EdgeOne（国内站）
> - 或者 DNS智能解析，按地区返回不同 IP

###4.7验证国内站

打开 https://pyq-cn.huanyan.org → 应该能秒开

---

## 🎯Step5：DNS智能路由（让用户访问最快）（20 分钟）

> 让用户访问 `pyq.huanyan.org` 时，**国内自动走 EdgeOne，国外自动走 CF**。

###5.1 在 Cloudflare DNS 配置

如果 huanyan.org 在 Cloudflare：
1. DNS → 加 A记录 `pyq` → EdgeOne分配的 IP（**Proxy 关掉**，灰色云朵）
2. DNS → 加 CNAME `pyq-cn` → EdgeOne 给的 CNAME
3. 用 **Load Balancing** 或 **Geolocation Routing**：
 - 中国大陆 → EdgeOne IP
 - 其他 → CF Pages 默认域名

###5.2 在 DNSPod 配置（如果域名在 DNSPod）

1. DNSPod → 加 A记录 `pyq` → EdgeOne IP（中国）
2. 加 A记录 `pyq` → CF Pages 的 anycast IP（境外）
3. 用 DNSPod 的**智能解析**功能按地区返回不同记录

---

## ✅Step6：上线后验证清单

跑完上面5步后，对照这个清单：

| 检查项 |怎么验证 |
|---|---|
| 🌍 国际站可访问 | `curl -I https://pyq.huanyan.org` 应该200 |
| 🇨🇳 国内站可访问 | `curl -I https://pyq-cn.huanyan.org` 应该200 |
| 📱移动端能看 |浏览器开发者工具 →切到 iPhone / Android → 看布局 |
| 🔗 Wikilink 能跳转 | 点 [[../内容营销朋友圈助手/00 - 内容营销朋友圈小助手（MOC入口）\|内容营销朋友圈小助手]] → 应该跳转 |
| 🤖 AI 生成按钮存在 |打开 AI Prompt库 →看到 "🤖 AI 生成"按钮 |
| 🤖 AI 生成能调通 |填表单 →看到返回的文案 |
| ⚡ HTTPS正常 |浏览器地址栏应该 🔒绿色锁 |
| 🚫 没有404 | 点几个 wikilink →都能跳转（除非笔记真不存在） |

---

## 🐛常见问题排查

###Q1：本地启动后中文乱码
A：检查 `quartz.config.ts` 里 `locale: "zh-CN"` 是否设置。

###Q2：wikilink跳转404
A：Quartz 默认对所有文件大小写敏感。Windows 文件系统不敏感，会出问题。建议：
-全部用 **小写文件名**
- 或部署到 Linux（CF Pages / EdgeOne 都是 Linux）

###Q3：AI 生成一直 loading
A：打开 DevTools → Network → 看 `/api/generate`响应：
-401 → API key错
-429 →触发 rate limit（等24 小时）
-500 → 后端报错（看日志）

###Q4：国内站访问慢
A：检查：
1.域名是否正确解析到 EdgeOne IP（`nslookup pyq-cn.huanyan.org`）
2. 是否启用了 HTTPS
3. 是否走了 Cloudflare代理（不要！会绕一大圈）

###Q5：CF Pages部署失败
A：看 CF Dashboard → Deployments →失败的那次 → Logs，常见原因：
- `package.json`缺依赖
- Build command路径错（应该是 `cd vendor/quartz && npm run build`）
- Node 版本太低（要在 CF 设置 `NODE_VERSION=20`）

---

## 🎉完成！

按这个指南走完，你的 `pyq.huanyan.org` 双站就上线了。

有任何步骤卡住 → **贴报错给我，我帮你 debug**。

---

## 📚参考文档

- [Quartz v4官方文档](https://quartz.jzhao.xyz/)
- [Cloudflare Pages文档](https://developers.cloudflare.com/pages/)
- [EdgeOne Pages文档](https://cloud.tencent.com/document/product/1659)
- [Claude API文档](https://docs.anthropic.com/)
- [minimax M3文档](#)（待你提供）